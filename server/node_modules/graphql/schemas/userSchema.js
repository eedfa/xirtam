const graphql = require('graphql')
const UserDBSchema = require('../mongoDBSchema/userSchema.js')
const { GraphQLObjectType, GraphQLInt, GraphQLString, GraphQLSchema } = graphql
const bcrypt = require('bcrypt')
const jsonwebtoken = require('jsonwebtoken')
const UserType = new GraphQLObjectType({

  name: 'login',
  fields: () => ({
    id: { type: GraphQLInt },
    username: { type: GraphQLString },
    password: { type: GraphQLString },
    token: { type: GraphQLString }

  })

})

const addUserMutaiton = {
  type: UserType,
  args: {
    username: { type: GraphQLString },
    passwordHash: { type: GraphQLString },
    email: { type: GraphQLString }
  },
  resolve (parent, args) {
    let user = new UserDBSchema({
      name: args.username,
      passwordHash: args.passwordHash,
      email: args.email })

    return user.save()
  }
}

const loginMutation = {
  type: UserType,
  args: {
    username: { type: GraphQLString },
    password: { type: GraphQLString }
  },
  resolve (parent, args) {
    const username = args.username

    const user = UserDBSchema.findOne({ where: { username } })

    const valid = bcrypt.compare(args.password, 'aaa')

    if (!valid) {
      throw new Error('Incorrect password')
    }
    const token = jsonwebtoken.sign({
      id: user.id,
      username: user.username
    }, 'jiodjwoijd9wjdopd203kJIO', { expiresIn: '1y' })
    return { token: token }
  }
}

module.exports = { UserType, addUserMutaiton, loginMutation }
